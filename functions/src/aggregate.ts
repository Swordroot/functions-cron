import * as adminModule from './classes/admin.js';
const admin = adminModule.admin;
const db = admin.firestore();
export const aggregate_rooms_per_user = event => {
  console.log();
}

export const aggregate_rooms_per_user_avg = event => {
  console.log();
}

export const aggregate_rooms_count_all = event => {
  console.log();
}